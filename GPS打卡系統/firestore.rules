rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gps_attendance/{document=**} {
      allow read, write: if request.time < timestamp.date(2030, 1, 1);
    }
  }
}
