rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to shop data
    match /shops/{shopId}/{document=**} {
      allow read, write: if true;
    }
  }
}
